{
  "name": "listen-and-speak",
  "version": "0.1.3",
  "description": "A custom web component for local, in-browser voice interaction featuring Voice Activity Detection (VAD), Speech-to-Text (STT), and Text-to-Speech (TTS)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "npx bendis --build-file js/speech/speaker-worker.js && npx bendis",
    "build": "export NODE_ENV=production || set NODE_ENV=production&& npx bendis --build-file js/speech/speaker-worker.js && npx bendis --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "config": {
    "prefix": "tts",
    "port": 3912
  },
  "bin": {},
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "bendis": "^1.2.3",
    "just-once": "^2.2.0",
    "p-limit": "^7.2.0"
  }
}
